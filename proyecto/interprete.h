/*
  Nombre: interprete.h
  Autor: Daniel Antonio Quihuis Hernandez
  Fecha: Noviemrbe del 2022
  Descripcion: este archivo contiene el codigo fuente del interprete para LETREC
*/

#include "analizador_semantico.h"


// Definiendo semantica
//(value-of (const-exp n) p) = (num-val n)
//(value-of(var-exp) p) = p(var)
//(value-of (diff-exp exp1 exp2) p)
  //=(num-val(-(exp-val -> num(value-exp exp1 p))
  //           (exp-val -> num(value-exp exp1 p))))

//

void interprete(){

system("title LETREC en C++ - Interprete LETREC");






}// Llave de cierre en la fucion interprete

